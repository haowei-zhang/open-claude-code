{
  "version": 1,
  "terms": [
    { "term": "tool", "definition": "A single-purpose capability exposed to the model via the tool dispatch pipeline. Each tool has a Zod/JSONSchema input schema, a call() method, concurrency flags, and optional deferral. Registered in src/Tool.ts." },
    { "term": "subagent", "definition": "A single-use agent invoked by the parent session via the Agent tool; has a fresh context; returns one result; does not persist across dispatches." },
    { "term": "fork", "definition": "A subprocess-based subagent execution mode where the agent runs in a forked process with isolated state, communicating via structured IPC. Implemented in src/utils/forkedAgent.ts." },
    { "term": "worktree", "definition": "A git worktree created as an isolated working directory for parallel agent execution, with its own branch and symlinked node_modules. Managed by EnterWorktreeTool/ExitWorktreeTool." },
    { "term": "task", "definition": "A durable unit of work with a status lifecycle (pending → in_progress → completed), blocking relationships, filesystem locking, and one of seven task types. Recorded in src/utils/tasks.ts." },
    { "term": "skill", "definition": "A reusable prompt template discovered from ~/.claude/skills/ or bundled, with YAML frontmatter metadata. Can be user-invocable (slash command) or triggered. Supports inline and fork context modes." },
    { "term": "slash command", "definition": "A user-typed command starting with / that is dispatched through the command registry in src/commands.ts. Can be a prompt template or a callback function." },
    { "term": "hook", "definition": "A deterministic lifecycle event handler registered in settings.json. Hook types: command, prompt, http, agent, function. Fires on PreToolUse, PostToolUse, and other lifecycle events. Defined by src/schemas/hooks.ts." },
    { "term": "permission mode", "definition": "A runtime mode governing how tool-use requests are authorized: default (ask), plan (read-only), acceptEdits, bypassPermissions, dontAsk, auto. Defined in src/utils/permissions/PermissionMode.ts." },
    { "term": "classifier", "definition": "A function that maps a bash command string to a risk band (safe, risky, destructive) to determine whether automatic approval is appropriate. Implemented in src/utils/permissions/bashClassifier.ts." },
    { "term": "compaction", "definition": "The process of reducing context size by summarizing or removing older messages. cc implements a five-stage hierarchy: history_snip → microcompact → context collapse → autocompact → hard reset." },
    { "term": "microcompact", "definition": "The second stage of compaction. Selectively summarizes individual messages or message groups while preserving key information. Implemented in src/services/compact/microCompact.ts." },
    { "term": "context collapse", "definition": "A severe compaction event where large portions of the conversation are replaced with a summary. Triggered when token usage approaches model limits." },
    { "term": "autocompact", "definition": "Automatic compaction triggered when the context window exceeds a threshold. Uses the model to generate a summary of the conversation so far. Implemented in src/services/compact/autoCompact.ts." },
    { "term": "memory", "definition": "A persistent knowledge record stored in the memdir filesystem. Types include user, feedback, project, and reference. Each has a frontmatter schema with name, description, and type fields." },
    { "term": "memdir", "definition": "The tiered on-disk memory system at ~/.claude/projects/<project>/memory/. Contains MEMORY.md index and per-memory .md files. Implements HER Pattern 3 Tiered Memory." },
    { "term": "session", "definition": "A single invocation of cc from startup to shutdown. Persisted as JSONL with entry types and tombstones. Supports resume via head/tail reading. Managed by src/utils/sessionStorage.ts." },
    { "term": "plan mode", "definition": "A read-only exploration phase where the agent can read files and search but cannot write or execute tools. Has five phases and enforced tool gating. Implemented in src/utils/planModeV2.ts." },
    { "term": "harness", "definition": "The engineering layer surrounding a language model that provides tools, context assembly, permission enforcement, lifecycle hooks, and state management. An agent = model + harness." },
    { "term": "sensor", "definition": "A harness component that observes agent behavior and feeds observations back into the context (e.g., file-read results, command outputs, debug logs)." },
    { "term": "guide", "definition": "A harness component that shapes the agent's behavior through prompts, CLAUDE.md files, skill templates, and system instructions." },
    { "term": "back-pressure", "definition": "A mechanism that slows or stops the agent's loop when resource limits (token budget, cost caps, rate limits) are approached, preventing unbounded resource consumption." },
    { "term": "deferred tool", "definition": "A tool that is not loaded into the model's tool list at session start but is discovered on-demand via ToolSearch. Implements HER Pattern 9 Progressive Tool Expansion." },
    { "term": "ToolSearch", "definition": "The tool-discovery mechanism that allows the model to search and load tools on demand. Implemented in src/tools/ToolSearchTool/." },
    { "term": "PreToolUse", "definition": "A lifecycle hook event that fires before a tool is executed. Can block, modify, or approve the tool call. Part of the hook schema in src/schemas/hooks.ts." },
    { "term": "PostToolUse", "definition": "A lifecycle hook event that fires after a tool has executed. Can inspect and modify the tool result before it reaches the model." },
    { "term": "manifest", "definition": "The build registry file (.book-build/manifest.json) containing every chapter's metadata, status, pass counts, and quality metrics. The single source of truth for build state." },
    { "term": "generator-evaluator", "definition": "A multi-agent pattern where one agent generates solutions and another evaluates them. Identified in HER §9.2 as a common orchestration pattern with an Ouroboros risk (self-reinforcing errors)." },
    { "term": "checkpoint-restore", "definition": "The ability to save and restore agent state at a known-good point. HER §6.16 identifies side effects (filesystem changes, API calls) as the primary obstacle to correct restoration." },
    { "term": "observation masking", "definition": "The practice of replacing verbose tool outputs with compressed summaries before feeding them back into the model context. HER §8.1 reports 52% cost reduction with this technique." }
  ],
  "pending_additions": []
}
