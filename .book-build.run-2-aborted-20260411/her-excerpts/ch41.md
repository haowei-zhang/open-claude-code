### 9. Progressive Tool Expansion
**Start with <20 tools; activate more on demand (not all 60+ at once)**

From the 12 Agentic Harness Patterns (Claude Code-Derived):

### Tools and Permissions (3 patterns)

9. **Progressive Tool Expansion**: Start with <20 tools; activate more on demand (not all 60+ at once)
10. **Command Risk Classification**: Deterministic pre-parsing and per-tool permission gating
11. **Single-Purpose Tool Design**: FileReadTool, FileEditTool, GrepTool each with individual permission rules

**Important caveat**: These patterns are derived from reverse-engineering Claude Code's architecture. They represent implementation decisions of one specific product (CLI-first, Anthropic-native). Other platforms make fundamentally different tradeoffs.

From Section 7.2 MCP Servers:
- "Too many tools is bad" -- excess tool descriptions push agents into "the dumb zone" faster
- If an MCP server duplicates CLI functionality well-represented in training data, use the CLI instead

From Section 7.3 Skills (Progressive Disclosure):
- Agents access specific instructions/tools only when contextually needed
- Reduces cognitive load by not front-loading all instructions

From Section 6.8 Tool Explosion:
- **Symptom**: Too many tools degrade selection accuracy
- **Fix**: Progressive tool expansion starting with <20

---

### 6.13 Security Vulnerabilities / Prompt Injection
- **Symptom**: Malicious content in files, tool outputs, or user inputs manipulates agent behavior. Can lead to data exfiltration, unauthorized operations, or system prompt leakage.
- **Evidence**: 73% of production AI deployments affected by indirect prompt injection in 2025. The OpenClaw incident (135,000 GitHub stars, 21,000 exposed instances, CVE-2025-53773, CVSS 9.6) is a major real-world case.
- **Fix**: Input sanitization; output filtering; least-privilege tool permissions; treat all external content as untrusted. See Section 12 for detailed security architecture.

**Prompt injection via MCP responses** is a specific attack vector: a malicious MCP server can return tool responses containing injected instructions that the agent follows as if they were legitimate context. Because the agent cannot reliably distinguish tool output from legitimate instructions, any MCP server response is a potential injection vector. Mitigation requires treating all MCP tool outputs as untrusted input, applying the same sanitization discipline used for file contents and user inputs.
