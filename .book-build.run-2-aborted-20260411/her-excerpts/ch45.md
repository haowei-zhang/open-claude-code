### 6. Explore-Plan-Act Loop
**Three phases with escalating permissions (read-only -> discussion -> full access)**

From the 12 Agentic Harness Patterns (Claude Code-Derived):

### Workflow and Orchestration (3 patterns)

6. **Explore-Plan-Act Loop**: Three phases with escalating permissions (read-only -> discussion -> full access)
7. **Context-Isolated Subagents**: Separate context windows per phase; research agents cannot edit
8. **Fork-Join Parallelism**: Multiple subagents in isolated git worktrees; cached parent context reuse

**Important caveat**: These patterns are derived from reverse-engineering Claude Code's architecture. They represent implementation decisions of one specific product (CLI-first, Anthropic-native). Other platforms make fundamentally different tradeoffs.

---

### 10.3 Session Protocol

Synthesized across Anthropic, OpenAI, and practitioner sources:

```
1. ORIENT     -- Read workspace state (pwd, git log, progress file)
2. SETUP      -- Run init script, install dependencies if needed
3. VERIFY     -- Run baseline tests/checks (CRITICAL: before any new work)
4. SELECT     -- Pick exactly one task from the task list
5. IMPLEMENT  -- Write code with frequent git commits
6. TEST       -- Run tests, lint, type-check (automated back-pressure)
7. UPDATE     -- Update task list status, progress notes, git commit
8. EXIT       -- Clean exit with state fully persisted
```

The Explore-Plan-Act loop maps onto the session protocol: ORIENT+SETUP+VERIFY corresponds to Explore (read-only), SELECT corresponds to Plan (discussion), and IMPLEMENT+TEST corresponds to Act (full access). The escalating permission model ensures the agent cannot make changes during the orientation phase, preventing premature action on incomplete understanding.
