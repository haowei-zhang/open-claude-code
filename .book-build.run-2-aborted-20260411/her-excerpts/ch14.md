# HER Excerpt for Chapter 14

## From §5: Pattern 10 Command Risk Classification

10. **Command Risk Classification**: Deterministic pre-parsing and per-tool permission gating

This pattern classifies every command and tool call by risk level before execution, using deterministic rules rather than model judgment. Each tool is assigned a risk category, and the harness enforces different approval requirements based on that classification:

- **Low-risk operations** (read-only): Auto-approved. File reads, searches, git log.
- **Medium-risk operations** (modifications with limited scope): May require implicit approval. File edits within allowed directories, test execution.
- **High-risk operations** (destructive or irreversible): Require explicit human approval. File deletions, force pushes, production deployments, external API calls.

**Key design principle**: Risk classification is deterministic. The harness parses the tool call parameters using fixed rules (not model judgment) to determine risk level. This prevents the model from downgrading its own operations to avoid approval gates.

The pre-parsing step examines tool call parameters before execution. For example, a BashTool call to `rm -rf /` would be classified as high-risk and blocked regardless of the model's stated intent. A BashTool call to `ls -la` would be classified as low-risk and auto-approved.

---

## From §6.16: Checkpoint-Restore Side Effects (ACRFence)

- **Symptom**: LLM agents re-synthesize subtly different requests after restore, causing duplicate payments, unauthorized credential reuse, and irreversible side effects.
- **Evidence**: The ACRFence paper (arXiv:2603.20625) identifies two attack classes -- Action Replay and Authority Resurrection -- specific to agent checkpoint-restore.
- **Fix**: Record irreversible tool effects; enforce replay-or-fork semantics; idempotency keys for external tool calls (see Section 12.6).
- **Critical for multi-hour tasks with external side effects (API calls, payments, email sends).**

---

## From §12.4: Security Threat Model

### 12.4 Security Threat Model for Agent Systems

This is a critical gap in most harness literature. For production multi-hour systems, the following threats must be addressed:

**Indirect Prompt Injection**: Malicious instructions embedded in files, tool outputs, web pages, or database records that the agent processes. The agent follows these injected instructions because it cannot reliably distinguish them from legitimate context.
- **Mitigation**: Input sanitization on all external content; treat file contents and tool outputs as untrusted; use deterministic validation (not LLM judgment) for security-critical decisions.

**Supply Chain Attacks via MCP Servers/Skills**: Compromised or malicious tools that the agent calls can exfiltrate data, modify behavior, or escalate privileges.
- **Mitigation**: Vet all MCP servers; restrict tool permissions to minimum required; audit tool call logs; run tools in sandboxed environments when possible.

**System Prompt Leakage**: Agent may be tricked into revealing its system prompt, harness configuration, or internal tool descriptions.
- **Mitigation**: Minimize sensitive information in system prompts; use post-processing filters on agent output.

**Data Exfiltration Through Tool Calls**: Agent sends sensitive data to external services via tool calls (API requests, file uploads, etc.).
- **Mitigation**: Allowlist external endpoints; monitor tool call parameters for sensitive data patterns; require human approval for external communications.
